# 결정 대기 항목

작성 2026-09-12 (2026-08-30 판 대체). **기존 `docs/` 조사 결과를 모은 문서다.** 권고는 쓰지 않는다.

**같은 날 갱신** — class_name 754종 교정(커밋 `ff921a2`)이 DB 에 반영되어, floor 와 Ne/Nc 숫자를
교정 후 DB 로 다시 셌다(`research/analyze_population_floor.ts` · `analyze_floor_peer_rank.ts` ·
`analyze_ne_nc_spec_vs_code.ts`). 교정 전 값은 괄호로 남겼다. 특허 명세서는 출원이 끝나
고칠 수 없으므로 명세서를 바꾸는 선택지는 **불가**로 표시했다.

`†` = 챗봇 비교 순위 영향. 비교 기능(커밋 `2101598`)이 floor 조사 이후에 생겨 기존 docs 에 없던 숫자라
두 사본(`research/_v5_replica.ts` / `_v5_replica_nofloor.ts`)으로 순위를 교차 집계한 값이다.

기준 모집단: 점수가 산출되는 **941종** 중 계산된 점수 **310종** (EX/EW 631종은 100 고정).

| # | 항목 | 지금 잘못 나가고 있는 종 |
|---:|---|---:|
| 1 | 개체수 floor | 점수 **48** · 비교 순위 **124†** (교정 전 47 · 121) |
| 2 | Damuth K 미도입 | **0** (적용 가능 종 0) |
| 3 | Ne/Nc 명세서 ≠ 코드 | Ne **277** · 점수 **47** (교정 전 156 · 18) |
| 4 | floor 의 LC/VU 예외 | **2** |
| 5 | class_name 754종 | **0** — 해결 (`ff921a2`) |
| 6 | damuthConstant 단위·fallback | **0** (호출 경로 없음) |

---

## 1. 개체수 floor

### 현상

`lib/tipping-point.ts` 605~618행. 개체수 N0 만 보고 종합 점수에 하한을 건다.
`Math.max` 라 점수를 올리기만 한다.

| 조건 | floor |
|---|---:|
| N0 < 50 | 90 |
| 50 ≤ N0 < 100 | 78 |
| 100 ≤ N0 < 250 | 70 |
| 250 ≤ N0 < 500 | 60 |

명세서 계열 5개 문서 전수 검색에서 **대응 기재 0건**. 근거는 도입 커밋 `d5525e0`(2026-05-04)
메시지에만 있다 (IUCN Criterion D, Frankham 50/500, 단일 멸종사건 취약성).

### 영향이 세 곳에 걸친다

| 영향 대상 | 종 수 | 내용 |
|---|---:|---|
| **① 점수** | **48** (교정 전 47) | floor 를 끄면 점수가 내려가는 종. 전부 큐레이티드. 티어가 갈리는 종 24 (교정 전 23) |
| **② Damuth K** | **25** | 48종 중 체중이 있어 Damuth 계산 대상이 될 수 있는 종. K 를 바꿔도 floor 가 덮어 점수 불변 |
| **③ 챗봇 비교 순위** | **124†** (교정 전 121) | floor 를 끄면 같은 등급·분류군 그룹 안 점수 순위가 바뀌는 종 (비교 블록 대상 289종 중) |

①은 `seed: 42` 로 센 값이고(`docs/population-floor-audit.md` 와 같은 방식), ③은 DB 와 같은
기본 시드로 센 값이다. ③에서 비교 블록 대상 289종 중 floor 에 묶인 종은 **40†** (교정 전 39),
floor 에 묶인 종이 있는 그룹은 **11†** 개다.

교정 후 새로 floor 에 묶인 종은 **마밀라리아 미크로헬리아(EN, N0 499)** 1종이다 — class_name 이
빈값에서 식물(쌍떡잎)로 바뀌어 하한 적용 전 점수가 floor 60 아래(52.2)로 내려갔다.
풀려난 종은 없다(백업 DB 와 대조). 묶인 48종 중 19종이 이번에 class_name 이 교정된 종이다.

③의 그룹별 내역† (교정 후, floor 에 묶인 종이 있는 그룹만):

| 그룹 | 종 수 | floor 에 묶임 | 순위 변동 |
|---|---:|---:|---:|
| CR 포유류 | 34 | 16 | **29** |
| CR 쌍떡잎식물 | 23 | 4 | 19 |
| EN 조류 | 21 | 3 | 19 |
| CR 조류 | 22 | 5 | 15 |
| EN 쌍떡잎식물 | 22 | 3 | 15 |
| LC 포유류 | 54 | 1 | 13 |
| CR 파충류 | 13 | 2 | 8 |
| EN 외떡잎식물 | 5 | 1 | 3 |
| CR 양서류 | 7 | 3 | 2 |
| CR 외떡잎식물 | 3 | 1 | 1 |
| CR 침엽수 | 3 | 1 | 0 |

CR 포유류는 13종이 floor 90 으로 **90.0점 동점**이다. 자바코뿔소 78.0 도 floor 값이며
(레이어 가중합은 46.65), 이 그룹의 "34종 중 17위" 는 floor 가 만든 순위다.

### 근거 문서

- `docs/population-floor-audit.md` — 규칙 전문, 47종(교정 전), 명세서 전수 검색, git 출처
- `docs/damuth-k-simulation.md` §7 — floor 에 묶인 47종 중 체중 보유 25종
- `docs/layer-score-spec-vs-code.md` §4 — 자바코뿔소 78.0 의 산출 경로
- `research/analyze_floor_peer_rank.ts` — ③ 비교 순위 교차 집계†

### 선택지와 영향 종 수

| 선택지 | 점수 변동 | 비교 순위 변동 |
|---|---:|---:|
| A. 명세서에 floor 를 추가 기재 | **불가** — 명세서 출원 완료 | — |
| B. 코드에서 floor 제거 | **48** (티어 이동 24: T3→T1 13, T3→T2 7, T4→T3 4) | **124†** |
| C. 현행 유지 + 챗봇 투명화 (**적용됨**, 커밋 `5c4222b`) | 0 | 0 |

C 는 점수를 그대로 두고, 챗봇이 하한 적용 여부와 하한 적용 전 점수·순위를 함께 밝히게 한 것이다.

## 2. Damuth K

### 현상

코드 K = `r < 0 ? max(N0×1.5, N0+100) : max(N0×1.2, N0+50)` — 체중·서식면적 입력 없음.
명세서(PKA-1551 C-1, 발명신고서 §8)는 Damuth 로 자바코뿔소 K ≈ 148, 코드는 126.
`lib/allometry.ts` 의 `damuthK()` 는 `evaluateTippingPoint` 가 호출하지 않는다 (호출처는 테스트뿐).
K 는 PVA 점수와 골든타임(h3) 날짜 산출 두 곳에 들어간다.

### 선행조건 3단계 — 각 단계 없이 진행하면

| 단계 | 현재 상태 | 이 단계 없이 도입하면 |
|---|---|---|
| ① 서식면적 데이터 | `habitat_area_km2` **컬럼 자체가 없음**, 보유 **0종** | `damuthK` 가 전 종에 `null` → **변동 0종**. 도입해도 아무 일도 안 일어난다 |
| ② kg/g 단위 (항목 6) | 상수 91.2 는 kg 기준, 함수는 g 을 받음 | K 가 **1/177.8 로 축소**. 자바코뿔소 K = **0.82** (N0 = 76 보다 작음) |
| ③ floor 재검토 (항목 1) | 미결정 | floor 에 묶인 종은 K 를 바꿔도 **점수 불변**. 자바코뿔소는 K 를 0.82~400 으로 500배 바꿔도 78.0 고정 (가중합 최대 72.16 < floor 78) |

### 근거 문서

- `docs/damuth-k-simulation.md` — K 사용처, 입력 가용성, 시뮬레이션, K 스윕, 단위 점검
- `docs/pka-1551-discrepancies.md` §2 — K 148 대조

### 선택지와 영향 종 수

| 선택지 | 점수 변동 |
|---|---:|
| A. 지금 도입 | **0** (서식면적 0종) |
| B. ①만 해결 후 도입 | 최대 210 (체중 보유 종). K 는 ② 때문에 1/177.8 로 산출됨 |
| C. ①② 해결 후 도입 | 최대 210. 단 floor 에 묶인 종(체중 보유 25)은 점수 불변 |
| D. ①②③ 해결 후 도입 | 최대 210, floor 결정에 따라 25종 포함 여부가 갈림 |
| E. 도입하지 않고 명세서를 코드에 맞춤 | **불가** — 명세서 출원 완료 |

## 3. Ne/Nc — 명세서 0.20/0.25/0.10 vs 코드 0.15/0.20/0.05

### 현상

| 분류군 | 코드 | 명세서 |
|---|---:|---:|
| 포유류 | 0.15 | **0.20** |
| 조류 | 0.20 | **0.25** |
| 파충류 | 0.15 | 0.15 |
| 양서류 | 0.10 | 0.10 |
| 어류 | 0.05 | **0.10** |
| 무척추 | 0.05~0.10 | **0.05** |
| 식물 | 0.20 | **0.10** |

`LIFE_HISTORY` 21개 키 중 **16개**가 다르다 (상승 5, 하락 11).
명세서 출처(`full_spec` v3 §4.1 · v4 §2.4 · 발명신고서 §8 · PKA-1551 C-1)는 서로 일치한다.
자바코뿔소 Ne: 코드 **11**(0.15), 명세서 실시예 **≈15**(0.20).

### 근거 문서

- `docs/ne-nc-spec-vs-code-2026-08-27.md` — 치환표, 941종 시뮬레이션, 자바코뿔소 대조
- `docs/patent/ne-nc-provenance-2026-08-27.md` — 저장소 내 기재 위치
- `docs/patent-spec-locations-2026-08-27.md` §3 — 문서별 값 대조

### 선택지와 영향 종 수

| 선택지 | Ne 변동 | 점수 변동 | 티어 변동 |
|---|---:|---:|---:|
| A. 코드를 명세서 값으로 | **277** (교정 전 156) | **47** (교정 전 18) | **10** (교정 전 3) |
| B. 명세서를 코드 값으로 | **불가** — 명세서 출원 완료 | — | — |
| C. 현행 유지 | 0 | 0 | 0 |

교정 후 선택지 A 의 내역 (`research/analyze_ne_nc_spec_vs_code.ts`):

| 항목 | 값 |
|---|---|
| Ne 가 바뀌는 종 | 277 — 포유류 145 · 식물(쌍떡잎) 58 · 조류 57 · 식물(침엽수) 8 · 식물(소철) 4 · 곤충 2 |
| 점수가 바뀌는 종 | 47 — 상승 27 · 하락 20 |
| 티어 이동 | 10 — T2→T3 6 · T1→T0 2 · T0→T1 1 · T3→T2 1 |

교정 전(Ne 156 · 점수 18 · 티어 3, 상승 1)에서 크게 늘었다. class_name 이 빈값이던 종은
`DEFAULT_LIFE`(0.15, 명세서 대응 없음)를 타서 치환 대상에서 빠져 있었는데, 교정으로 식물·조류
분류군 상수를 갖게 되어 대상에 들어왔다. 식물 계열은 명세서 값(0.10)이 코드 값(0.20)보다 낮아
치환하면 Ne 가 줄어든다. 항목 5 → 3 의존이 실제로 나타난 것이다.

Ne 가 바뀌어도 점수까지 움직이는 종이 적은 것은 `evaluateIucn` 이 Ne 50/100/500/1000 계단 함수라
구간을 넘지 않으면 점수가 같기 때문이다. 자바코뿔소는 명세서 값 적용 시 Ne 15 로 명세서 실시예와 일치한다.

## 4. floor 의 LC/VU 예외

### 현상

floor 는 등급을 보지 않아 위협등급이 아닌 종도 N0 < 500 이면 T3 가 된다.
floor 에 묶인 48종의 등급 (교정 후): CR 39 · EN 7 · **VU 1** · **LC 1**. VU·LC 두 종은 교정 전과 같다.

| 종 | 등급 | N0 | floor | 현재 | floor 끄면 |
|---|---|---:|---:|---|---|
| *Balaena mysticetus* (북극고래) | **LC** | 258 | 60 | **60.0 / T3** | 23.3 / T1 |
| 갈로티아 시모니 | **VU** | 350 | 60 | **60.0 / T3** | 39.6 / T1 |

위 표의 "floor 끄면" 값은 `docs/population-floor-audit.md` 의 `seed: 42` 값이다.
DB 와 같은 기본 시드로 계산하면 북극고래는 **23.2**† 다 (PVA 몬테카를로 시드 차이).

북극고래는 챗봇 비교 블록의 **LC 포유류 그룹(54종)** 에 들어 있고, 이 그룹에서 floor 에
묶인 유일한 종임을 확인했다 (†, 항목 1 ③ 표의 LC 포유류 행). 갈로티아 시모니는 VU 파충류 그룹이
2종이라 비교 블록이 붙지 않는다 (`research/analyze_peer_groups.py`).

### 근거 문서

- `docs/population-floor-audit.md` §3·§4

### 선택지와 영향 종 수

| 선택지 | 점수 변동 |
|---|---:|
| A. floor 에 등급 조건 추가 (CR/EN 에만 적용 등) | **2** |
| B. floor 전체 제거 (항목 1-B) | 48 |
| C. 현행 유지 | 0 |
| D. 두 종의 개체수 데이터 재검토 | 확인 결과에 달림 |

선택지 A 는 `01fda1b`(v5) 가 "순환 방지" 를 이유로 제거한 카테고리 조건을 되돌리는 방향이다.

## 5. class_name 754종 — **해결됨** (2026-09-12, 커밋 `ff921a2`)

### 해결 결과

| 항목 | 값 |
|---|---|
| 교정 | 754종 (불일치 27 + 빈값 727), 남은 대상 0 |
| 행 수 | species 41,282 → 41,282 · tipping_points 941 → 941 |
| DB MD5 | `93e284e439583a4727338a298df2b548` → `a751d1f9d8b515143c0d5b6d9b7a03f2` |
| 대상 밖 변동 | **0** (payload 까지 백업과 대조) |
| 점수·티어·Ne 변동 | **119** — DB 시드 사전 예측과 종 집합까지 일치 |
| 점수 | 하락 30 · 상승 0 · Ne 만 89 |
| 티어 이동 | 10 — T1→T0 2 · T3→T2 8 |

재현·검증: `research/apply_class_name_fix.py` · `research/verify_class_name_fix.py`.
교정으로 항목 1(floor)과 항목 3(Ne/Nc)의 숫자가 바뀌어 위에 갱신했다. 아래는 해결 전 기록이다.

### 현상

큐레이티드 종 중 `class_name` 이 `iucn_class` 와 어긋나거나 비어 있는 종이 754종.
`lifeFor()` 가 `class_name` 으로 생활사 파라미터(`ne_nc` 포함)를 고르므로 틀린 값으로 계산된다.

| 구분 | 종 수 |
|---|---:|
| 불일치 | 27 (파충류→조류 24 등) |
| 빈 값 → `DEFAULT_LIFE` | 727 |
| 합계 | **754** |
| 그중 점수 산출 | 383 |
| **그중 점수·티어·Ne 가 바뀌는 종** | **119** |

(해결 전) 수정 스크립트가 승인 분류기에 3회 거부되었다가 2026-09-12 에 적용되었다.
백업 확보: `data/species.db.backup_before_class_name_fix_20260830-124547` (교정 전 원본과 MD5 동일).

챗봇 비교 블록은 이 문제를 피하려고 `class_name` 대신 `iucn_class` 로 그룹을 나눈다.

### 근거 문서

- `docs/class-name-fix-impact-2026-08-27.md` — 시뮬레이션 전문
- `docs/data-quality-suspects-2026-08-27.md` §1 — 27종 목록

### 선택지와 영향 종 수

| 선택지 | 점수 변동 |
|---|---:|
| A. 754종 전부 교정 — **적용됨** | **119** (불일치 21 + 빈값 98; 하락 30 · 상승 0 · Ne 만 89; 티어 이동 10, 전부 하향) |
| B. 불일치 27종만 | 21 |
| C. 빈값 727종만 | 98 |
| D. 현행 유지 | 0 |

선택지 A 를 적용했다.

## 6. damuthConstant — kg/g 단위 + 17개 분류군 fallback

### 현상 — 단위

`docs/phase2-allometry-constants.md` 는 "W = grams" 라 적고 포유류 d = 91.2 를 싣는다.
91.2 는 **kg 기준 상수**다.

| 계산 | 결과 |
|---|---:|
| 명세서 `91.2 × 2000^(−0.75) × 480` (kg) | 146.4 |
| 코드 `damuthK(2_000_000, "포유류", 480)` (g) | **0.82** |
| Damuth 1981 원식 `10^4.23 × 2e6^(−0.75) × 480` (g) | 153.3 |

`1000^0.75 = 177.8`, `91.2 × 177.8 = 16,218` ↔ 원논문 절편 `10^4.23 = 16,982`.
`__tests__/allometry.test.ts:37` 이 이 짝을 기대값으로 고정하고 있다.

### 현상 — fallback

전용 상수는 4개 (포유류 91.2 / 조류 55.0 / 파충류 12.0 / 양서류 200).
v5 `LIFE_HISTORY` 21개 분류군 중 **17개**가 포유류 91.2 로 fallback 한다 —
어류 3계열·곤충·거미류·갑각류·복족류·이매패류·산호류·식물 4계열·양치식물 2계열·이끼류·지의류.

### 근거 문서

- `docs/damuth-k-simulation.md` §2·§5
- `docs/phase2-allometry-constants.md` §3

### 선택지와 영향 종 수

| 선택지 | 지금 점수 변동 | 항목 2 도입 시 |
|---|---:|---|
| A. 상수를 g 기준으로 교체 | 0 | 전 종 K 177.8 배 (정상 스케일) |
| B. 함수 입력을 kg 으로 변경 | 0 | 위와 같음 |
| C. 17개 분류군 전용 상수 조사 | 0 | 체중 보유 210종 중 `class_name` 빈값 33종이 fallback 대상 |
| D. 현행 유지 | 0 | K 가 1/177.8 로 산출됨 |

**현재 점수 영향은 0** — `evaluateTippingPoint` 가 `lib/allometry.ts` 를 호출하지 않는다.
항목 2 를 진행할 때만 영향이 생긴다.

---

## 의존 관계

| 선행 → 후행 | 이유 |
|---|---|
| **6 → 2** | 단위를 고치지 않고 Damuth K 를 켜면 K 가 1/177.8 로 산출된다 |
| **1 → 2** | floor 가 남아 있으면 floor 에 묶인 종은 K 를 바꿔도 점수가 변하지 않는다 |
| **1 → 4** | 4 는 1 의 부분 문제다. 1 을 B(제거)로 정하면 4 는 자동 해소된다 |
| **5 → 3** | 둘 다 `lifeFor()` 가 고르는 생활사 파라미터다. 5 로 분류군이 바뀌면 3 에서 적용되는 `ne_nc` 가 달라져 3 의 영향 종 집합이 바뀐다. **5 해결로 실제로 바뀌었다** (점수 변동 18 → 47) |

2 의 선행조건 ① 서식면적 데이터는 다른 항목에 의존하지 않는 **독립 선행조건**이다.
5 → 3 은 순서 관계일 뿐, 5 없이 3 을 결정하는 것이 불가능하지는 않다 (3 의 영향 종 집합이 달라질 뿐이다).

선행 항목이 없어 다른 결정을 기다리지 않는 항목: **1 · 6 · 2①** (5 는 해결됨).
선행 항목이 있는 항목: **2** (1 · 6 · 2① 필요), **4** (1). **3** 은 선행 항목 5 가 해결되어 이제 바로 결정할 수 있다.

## 검증

| 확인 항목 | 결과 |
|---|---|
| `lib/tipping-point.ts` | 무변경 |
| `data/species.db` | 이 문서 작성 중 무변경. 현재 MD5 `a751d1f9d8b515143c0d5b6d9b7a03f2` (class_name 교정 `ff921a2` 반영 후) |
| † 값의 검산 | `_v5_replica.ts` 점수와 DB `tipping_points` 점수 불일치 0종 |
